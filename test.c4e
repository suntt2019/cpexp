long labs(long x);
long printf(string format, ...);

long a = -1;

long add(long a, long b) {
    return a + b;
}

long main() {
    long b = 1;
    b = add(a, b);
    if(b>0) {
        b = b + 10;
    }
    b = labs(a);
    asm("xor rax, rax");
    printf("%ld %ld\n", a, b);
//    print_int(b);
    return 0;
}
