long labs(long x);
int printf(string format, ...);

int a = -1;

int add(int a, int b) {
    return a + b;
}

int main() {
    int b = 1;
    b = add(a, b);
    if(b>0) {
        b = b + 10;
    }
    b = labs(a);
    int d = printf("%ld %ld\n", a, b);
    printf("%d\n", d);
    return 0;
}
