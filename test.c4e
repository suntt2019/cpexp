long print_int(long x);

long a = 1;

long add(long a, long b) {
    return a + b;
}

long main() {
    long b = 1;
    b = add(a, b);
    if(b>0) {
        b = b + 10;
    }
    print_int(b);
    return 0;
}
