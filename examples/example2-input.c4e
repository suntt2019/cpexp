long labs(long x);
int printf(string format, ...);

int a = -1;

int add(int a, int b) {
    return a + b;
}

int main() {
    bool bb;
    bb = 4 + a;
    int b = bb;
    b = add(a, b);
    // b = 2
    if(b>0 && printf("this shouldn't print\n", b)) {
        b = b + 10;
    }
//    b = labs(a);
    char ch = 'a' + 1;
    int d = printf("%ld %ld %c %d\n", a, b, ch, bb);
    printf("%d\n", d);
    return 0;
}
